name: CI vzlogger2mqtt

on:
  pull_request:
    paths:
      - "vzlogger2mqtt/**"
      - ".github/workflows/ci-vzlogger2mqtt.yml"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate JSON files
        run: |
          jq empty vzlogger2mqtt/config.json
          jq empty vzlogger2mqtt/build.json

      - name: Shell syntax check
        run: bash -n vzlogger2mqtt/run.sh

      - name: Shellcheck
        uses: ludeeus/action-shellcheck@2.0.0
        with:
          scandir: vzlogger2mqtt

      - name: Hadolint (Dockerfile lint)
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: vzlogger2mqtt/Dockerfile
          # DL3006: ARG-based FROM is standard HA addon practice
          # DL3018: apk version pinning impractical with HA base images
          # DL3003: cd within a single RUN layer is intentional here
          ignore: DL3006 DL3018 DL3003

  build-amd64:
    name: Build amd64 (no push)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Read BUILD_FROM for amd64
        id: meta
        run: |
          echo "build_from=$(jq -r '.build_from.amd64' vzlogger2mqtt/build.json)" >> "$GITHUB_OUTPUT"

      - name: Build (amd64 only, no push)
        uses: docker/build-push-action@v6
        with:
          context: vzlogger2mqtt
          push: false
          build-args: |
            BUILD_FROM=${{ steps.meta.outputs.build_from }}
          cache-from: type=gha,scope=amd64-vzlogger2mqtt
          cache-to: type=gha,scope=amd64-vzlogger2mqtt,mode=max
